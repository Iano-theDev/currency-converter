export interface IMainConfig {
    port: string ,
    pg_user: string,
    pg_pass: string,
    pg_host: string,
    pg_db: string,
    pg_port: number,
    exrate_api_key: string,
    exrate_api_url: string
    app_url: string
}
